[package]
name = "chronoscope-workers"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
## Record HTTP fixtures for VCR-style tests (hits real network).
## Usage: cargo test -p chronoscope-workers --features record-fixtures
record-fixtures = []

[dependencies]
# Internal crates
chronoscope-analysis = { path = "../analysis" }
chronoscope-db = { path = "../db" }
chronoscope-integrations = { path = "../integrations" }

# Async runtime
tokio.workspace = true

# Async
async-trait = "0.1"
bytes.workspace = true

# HTTP types (StatusCode used in URL fetcher error handling)
reqwest = { version = "0.12", default-features = false }

# Hashing (for content storage keys)
sha2 = "0.10"
hex = "0.4"

# Utilities
chrono.workspace = true
thiserror.workspace = true
uuid.workspace = true
url.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing = "0.1"

# Content extraction and processing
readability-js = "0.1"
html2md = "0.2"
scraper = "0.22"
infer = "0.19"

# Image processing
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "gif", "webp"] }
kamadak-exif = "0.6"
image_hasher = "3.0"

# Video metadata
mp4parse = "0.17"
# matroska removed: requires file path, not in-memory buffer; WebM parsing not yet implemented

[dev-dependencies]
tempfile = "3"
# Enable testing utilities (MockHttpClient) for unit tests
chronoscope-integrations = { path = "../integrations", features = ["testing"] }
# Enable MockTritonService for analysis worker tests
chronoscope-analysis = { path = "../analysis", features = ["testing"] }
