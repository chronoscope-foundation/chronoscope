# Core dependencies for Triton model testing
numpy>=1.24.0
pillow>=10.0.0

# Model dependencies (same as Triton server runtime)
torch>=2.0.0
transformers>=4.40.0
# SAM3 fork with MPS/CPU support (upstream PR: facebookresearch/sam3#400)
# setuptools<81: sam3 uses pkg_resources (removed in setuptools>=81)
# Undeclared transitive deps of sam3:
# setuptools<81: sam3 uses pkg_resources (removed in setuptools>=81)
setuptools<81
einops
pycocotools
psutil
sam3 @ git+https://github.com/Shreesh-Coder/sam3.git@feature/macos-cpu-mps

# Testing framework
pytest>=8.0.0
hypothesis>=6.100.0
jsonschema>=4.20.0

# Code quality
ruff>=0.4.0
mypy>=1.10.0
